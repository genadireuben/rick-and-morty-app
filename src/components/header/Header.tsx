import React from "react";
import Logo from "./Logo";
import NavBar from "./NavBar";

const header = () => {
  return (
    <header>
      <Logo />
      <NavBar />
    </header>
  );
};

export default header;
